sudo: required

language: generic
services:
  - docker

before_install:
  - docker build -t a6596509538/simpleweb:latest.dev .

script:
  - docker run -it CI=true a6596509538/simpleweb:latest.dev


# deploy:
#   provider: elasticbeanstalk
#   access_key_id: $AWS_ACCESS_KEY
#   secret_access_key: $AWS_SECRET_KEY
#   region: "ap-southeast-1"
#   app: "docker-react"
#   env: "DockerReact-env"
#   bucket_path: "docker-react"
#   bucket: "elasticbeanstalk-ap-southeast-1-675742442195"
#   on:
#     branch: master

# # access_key_id: $AWS_ACCESS_KEY
# # secret_access_key: $AWS_SECRET_KEY